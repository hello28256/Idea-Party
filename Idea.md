## 角色

你是一个资深全栈工程师，擅长 Vue 3、Spring Boot、LangChain、WebSocket、AI Agent 编排和产品级 UI 设计。

## 项目目标

开发一个 AI 多角色聊天室平台。

用户可以在一个对话框中同时和多个 AI 角色对话，类似群聊或圆桌讨论。用户可以添加历史人物、作家、科学家、企业家、演员、明星或自定义角色。

系统需要能够根据角色名称，自动从互联网检索公开信息、作品、观点、语言风格，并生成该角色的系统提示词。用户不需要手动写 prompt。

系统中需要有一个 Moderator Agent，用来判断下一轮应该由哪个角色发言，使对话更自然、有逻辑，而不是所有角色机械轮流回答。

## 核心功能

1. 用户创建聊天室
2. 用户输入讨论主题
3. 用户添加多个 AI 角色
4. 系统自动生成角色画像和角色 prompt
5. 用户发送消息后，后端调用 AI 模型生成回复
6. Moderator Agent 判断下一位发言角色
7. AI 角色以聊天室消息流形式回复
8. 支持流式输出
9. 保存聊天记录
10. 保存角色信息和生成的 prompt

## 技术栈

前端：
- Vue 3
- Vite
- TypeScript
- Pinia
- Vue Router
- CSS 动画 / Vue Transition
- 页面风格：现代、专业、健康积极、有轻微科技感

后端：
- Spring Boot
- WebSocket / Socket.IO
- LangChain4j 或 LangChain 相关编排思想
- MySQL 或 PostgreSQL
- Redis 可选

AI 调用：
- DeepSeek V4
- 使用 OpenAI SDK 兼容格式
- 后端统一封装 AIService
- 不允许前端直接暴露 API Key

联网检索：
- 使用 Firecrawl 搜索和抓取公开网页信息
- 检索结果需要摘要、去重、过滤低质量来源
- 生成角色 prompt 时必须说明这是“基于公开资料模拟的角色”，不能声称是真人本人

## 安全与合规要求

1. 不允许让 AI 角色声称自己就是真实人物本人
2. 对在世名人、演员、明星，只能模拟公开形象和公开观点
3. 不生成侵犯隐私、诽谤、未证实八卦内容
4. 角色 prompt 中必须包含免责声明：
   “这是基于公开资料生成的 AI 模拟角色，并非本人。”

## 页面要求

1. 页面必须美观专业
2. 配色积极、健康、明亮
3. 聊天室需要支持多角色头像、角色名、发言状态
4. 需要显示“当前正在思考的角色”
5. 需要有角色添加面板
6. 需要有聊天室主区域
7. 需要有角色列表侧边栏
8. 移动端要基本可用

## 开发要求

1. 开发前先阅读当前项目结构
2. 不要盲目重构已有代码
3. 每完成一个模块都要自主测试
4. 如果发现现有代码和目标冲突，先说明问题，再给出修复方案
5. 必须生成完整可运行代码
6. 前后端接口必须真实打通，不允许只写 mock
7. 如果暂时不能接入真实 Firecrawl 或 DeepSeek，需要先封装接口，并提供 mock fallback，方便后续替换


## 生成的文档要求

1. 所有GSD生成文档尽量都使用中文



## 测试提示词
请不要只让我手动验证。请使用可用的 MCP 工具或已安装的 testing/browser/playwright skill 来自动测试这个功能。

目标：
1. 打开前端页面
2. 登录或注册测试用户
3. 创建聊天室
4. 添加一个 AI 角色
5. 发送一条消息
6. 验证 AI 回复是否以 streaming 形式出现
7. 如果失败，请定位是前端、API、WebSocket 还是数据库问题
8. 修复后重新测试，直到通过

要求：
- 优先使用 Playwright MCP / browser MCP
- 如果没有 MCP，则创建 Playwright E2E 测试脚本
- 不要只做 curl 测试，必须验证真实浏览器流程
- 测试结束后输出测试结果、失败原因和修改文件
